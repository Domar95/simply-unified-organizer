import { ProgrammingProjectStrategy } from './programming-project-strategy';

describe('ProgrammingProjectStrategy', () => {
  it('should create an instance', () => {
    expect(new ProgrammingProjectStrategy()).toBeTruthy();
  });
});
